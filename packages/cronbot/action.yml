# action.yml
name: "cronbot"
description: "harvest data for all the bathingspots"
# inputs:
#   who-to-greet:  # id of input
#     description: 'Who to greet'
#     required: true
#     default: 'World'
# outputs:
#   time: # id of output
#     description: 'The time we greeted you'
runs:
  using: "docker"
  image: "./dockerfile-actions"
  # image: technologiestiftung/flusshygiene-cronbot:v2.10.5-dev

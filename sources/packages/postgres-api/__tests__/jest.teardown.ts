module.exports = async () => {
  // try {
  //  const connection = await getConnection();
  //  connection.close();
  // } catch (error) {
  //   throw error;
  // }
  // console.log('teardown jest for all tests');
};
